import React from 'react';

const Spinner: React.FC = () => {
    return (
        <div className="w-16 h-16 border-4 border-green-600 border-t-0 rounded-full animate-spin"></div>
    );
};

export default Spinner;
